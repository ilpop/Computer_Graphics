#version 330

in vec3 vertexColor; // Input variable for vertex color
out vec4 FragColor; // Output color


void main() {
    // Output the interpolated vertex color
    FragColor = vec4(vertexColor, 1.0);
}