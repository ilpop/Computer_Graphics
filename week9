void mainImage( out vec4 fragColor, in vec2 fragCoord ) {
    vec4 data = texelFetch(iChannel0,
     ivec2(fragCoord.xy - 1.5), 0).rgba;
    data.rgb *= 1.0/data.a;
    // Output to screen
fragColor = vec4(pow(data.rgb, vec3(1.0 / 1.5)), 1.0); }